<UserControl x:Class="ChatBubbleClientWPF.Controls.NotificationTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatBubbleClientWPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="180" Margin="3,0,3,3">
    <Grid>
        <Rectangle Fill="{Binding NotificationFill, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:NotificationTile}}}" 
                   Stroke="{Binding BorderBrush, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:NotificationTile}}}" 
                   RadiusX="16" RadiusY="16"/>
        <StackPanel>
            <Label Padding="16,4,0,0"
                   Content="{Binding Header}"
                   Style="{StaticResource MediumTextStyle}" FontSize="14"/>
            <Label Padding="16,0,0,0"
                   Content="{Binding Content}"
                   Style="{StaticResource SmallTextStyle}" FontStyle="Italic" FontSize="10"/>
        </StackPanel>
    </Grid>
</UserControl>
